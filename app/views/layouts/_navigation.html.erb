
  <div class="bg-dark navbar-dark text-white">
    <div class="container">
      <nav class="navbar px-0 navbar-expand-lg navbar-dark">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
            <%= link_to "PTTT", root_path, class: "p-3 text-decoration-none text-light" %>
            <%= link_to "New Piece", new_piece_path, class: "p-3 text-decoration-none text-light" %>
            <%= link_to "Pieces", pieces_path, class: "p-3 text-decoration-none text-light"%>
            <%= link_to "Categories", categories_path, class: "p-3 text-decoration-none text-light"%>
            <%= link_to "Users", users_path, class: "p-3 text-decoration-none text-light"%>
            <% if logged_in? %>
              <%= link_to "Logout", logout_path, class: "p-3 text-decoration-none text-light", method: :delete%>
              <% if !current_user.admin?%>
                <strong><%= current_user.username %> is Logged in.</strong>
              <% else %>
                <strong> [Admin] <%= current_user.username %> is Logged in.</strong>
              <% end %>
            <% else %>
              <%= link_to "Signup", signup_path, class: "p-3 text-decoration-none text-light"%>
              <%= link_to "Login", login_path, class: "p-3 text-decoration-none text-light"%>
            <% end %>
          </div>
        </div>
      </nav>
    </div>
  </div>
